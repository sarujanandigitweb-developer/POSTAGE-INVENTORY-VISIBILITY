# Credentials. NEVER commit these, and never let them reach dashboard/ — the dashboard
# is published to the Varman AIOS hub and is readable by anyone with the link.
.env
*.env
!example.env
node_modules/

# Generated by the 2-hourly refresh — reproduced on every run, never committed.
logs/
sql/refresh/out/
sql/refresh/compare-*.json
dashboard/*.bak
